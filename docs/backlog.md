# Backlog

The current pass resolved the verified remote-image request path, Gmail page-storage fallback, stale/cloned button binding, active Compose cleanup, automated browser baseline, and deterministic package allowlist. The following items remain.

| Priority | Title                                    | User or technical impact                                                                                    | Evidence                                                                                        | Proposed solution                                                                                                                                              | Implementation risk                                                | Size | Product decision required |
| -------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ---- | ------------------------- |
| P1       | Run real Gmail release matrix            | Private Gmail DOM changes can still block the core workflow despite local tests.                            | Automated tests use `qa/harness.html`, not a mailbox.                                           | Execute and record `TEST_CASES.md` on Chrome Stable in both supported Gmail locales.                                                                           | Low; manual access and disciplined evidence are required.          | M    | No                        |
| P1       | Add unpacked-extension smoke test        | Page-script tests do not prove isolated-world behavior, manifest injection, or real `chrome.storage.local`. | Browser suite loads `content.js` from the harness.                                              | Launch a persistent Chrome context with the unpacked extension and a local approved test page or controlled Gmail session; keep credentials out of automation. | Medium; extension loading and Gmail access can make tests brittle. | M    | Yes                       |
| P1       | Validate image boundary in real Gmail    | Gmail may proxy, rewrite, or replace image URLs and Blob URLs differently from the fixture.                 | Current controls are verified against synthetic `https`, relative, `blob`, and `data` behavior. | Inspect DevTools Network with a controlled draft and document source-vs-preview requests.                                                                      | Medium; must avoid real tracking URLs and personal content.        | S    | No                        |
| P1       | Choose an explicit software license      | Outside users cannot safely reuse or contribute to a public repository without clear rights.                | No `LICENSE` file exists.                                                                       | Maintainer selects a standard license consistent with the intended non-commercial/open-source posture, with legal review if needed.                            | High legal/product implications; do not choose automatically.      | S    | Yes                       |
| P2       | Native Gmail comparison evidence         | Device chrome and Dark Mode remain approximations without versioned comparison artifacts.                   | `DEVICE_RESEARCH_AND_VALIDATION.md` defines a protocol but no complete evidence set is tracked. | Run representative iOS/Android fixtures and record versions, settings, and differences.                                                                        | Medium; access to physical/hosted devices is needed.               | M    | No                        |
| P2       | Screen-reader and 200% zoom matrix       | Accessible names/focus/status are implemented but not comprehensively qualified.                            | Automated tests assert focus and live-region basics only.                                       | Test VoiceOver, keyboard-only use, reduced motion, and 200% zoom; fix verified blockers.                                                                       | Low to medium.                                                     | M    | No                        |
| P2       | Chrome `_locales` integration            | Extension metadata and browser-managed localization are not available beyond in-product strings.            | Manifest has hard-coded English name/description.                                               | Add `_locales/en` and `_locales/zh_TW`, then convert manifest strings to message keys.                                                                         | Medium; Store copy and fallback behavior need review.              | M    | Yes                       |
| P2       | Formatting-risk calibration              | Current thresholds may produce false positives or miss complex color/layout failures.                       | Contrast, tiny text, blank lines, width, and string checks are heuristic.                       | Build anonymized fixtures, record false positives/negatives, and adjust only from evidence.                                                                    | Medium; careless tuning creates fake precision.                    | M    | No                        |
| P2       | RTL content qualification                | Sanitized `dir` is retained, but full RTL behavior is not verified.                                         | No automated or manual RTL fixture in the current suite.                                        | Add Arabic/Hebrew mixed-content fixtures and document shell limitations.                                                                                       | Low.                                                               | S    | No                        |
| P2       | Error-state UX for Gmail incompatibility | Users may see no Preview control when Gmail selectors break and receive no explanation.                     | A content script cannot easily surface a message if no Compose candidate is recognized.         | Add a popup diagnostic state based on a minimal, privacy-safe heartbeat only if it can avoid broad permissions and mailbox data.                               | Medium; may require message passing or storage state.              | M    | Yes                       |
| P3       | Extract pure sanitizer/audit modules     | The runtime remains large and coupled, increasing review cost.                                              | `content.js` owns most responsibilities.                                                        | Extract pure/testable boundaries incrementally after behavior coverage is stable.                                                                              | Medium; a broad rewrite could regress Gmail integration.           | L    | No                        |
| P3       | Visual regression baseline               | Visual regressions rely on manual review and existing screenshots.                                          | No automated screenshot comparison gate exists.                                                 | Add a small stable set for the local harness only; avoid device/client claims.                                                                                 | Low to medium; font/render differences can create noise.           | M    | Yes                       |
